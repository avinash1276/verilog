module dlatch_tb;

reg d, en;
wire q;

dlatch dut(
    .d(d),
    .en(en),
    .q(q)
);

initial begin
    $dumpfile("dlatch.vcd");
    $dumpvars(0, dlatch_tb);

    $monitor("time=%0t, en=%b, d=%b, q=%b", $time, en, d, q);

    en = 0; d = 0;
    #10 d = 1;
    #10 en = 1;
    #10 d = 0;
    #10 d = 1;
    #10 en = 0;
    #10 d = 0;
    #10 en = 1;
    #10 $finish;
end

endmodule